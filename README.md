# CTU-Simple-ATM-System
Simple ATM Console Application using C# that simulates a basic banking withdrawal  transaction. The system must interact with the user, accept inputs, perform calculations, and  display transaction results including the updated balance and transaction time
